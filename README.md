This python script execute a simple routine computing forecasting for a simple time serie. It requires as few input as possible (csv, name of the serie column within the csv and name of the variable that carries the t
date(or period)). 
It checks whether the serie needs to be differentiated or not. 
